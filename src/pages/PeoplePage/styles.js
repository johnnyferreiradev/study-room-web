import styled from 'styled-components';

const StyledPeoplePage = styled.div.attrs(() => ({
  classNames: 'people-page',
}))`
  width: 100%;
  display: flex;
  justify-content: center;
`;

export default StyledPeoplePage;
